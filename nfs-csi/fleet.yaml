defaultNamespace: nfs-csi
helm:
  chart: https://helm.familyhartley.com/csi-driver-nfs-4.11.0.tgz

targetCustomizations:
- name: dev
  helm:
    values:
      storageClass:
        create: true
        name: nfs
        parameters:
          server: 192.168.3.250
          share: /volume1/kubernetes
        reclaimPolicy: Delete
        volumeBindingMode: Immediate
        mountOptions:
          - nfsvers=4
  clusterSelector:
    matchLabels:
      env: dev

- name: prod
  helm:
    values:
      storageClass:
        create: true
        name: nfs
        parameters:
          server: 192.168.3.250
          share: /volume1/kubernetes
        reclaimPolicy: Delete
        volumeBindingMode: Immediate
        mountOptions:
          - nfsvers=4
  clusterSelector:
    matchLabels:
      env: prod